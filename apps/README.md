# apps

Blueprint application boundaries. `frontend-angular`, `gateway-dotnet`, and `orchestrator-langgraph` are migration targets; current runtime remains in compatibility paths until parity gates pass.
