# Governed router migration

This marker exists only to trigger the one-time GitHub Actions migration that installs the `tools` router boundary in `server/routers.ts`. It can be removed after the committed router integration is verified.
