# AngelMind Operations Runbook

Monitor API health, queue depth, worker health, database availability, edge errors and notification delivery.

For incidents: acknowledge → assess scope → contain affected workflow → preserve audit/evidence → recover → verify → document follow-up actions.

High-risk execution remains disabled when scope, authorization, policy or required dependencies are unavailable.
