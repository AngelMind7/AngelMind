# src/c2

Blueprint command-and-control domain boundary. Existing TypeScript server/runtime modules are mapped here incrementally; no blind rename is allowed because import and deployment entrypoints are active.
