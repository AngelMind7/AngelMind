import { NeonFrame, Eyebrow } from "@/components/NeonFrame";
import { Button } from "@/components/ui/button";
import { trpc } from "@/lib/trpc";
import { FileLock2, Hash, ShieldCheck } from "lucide-react";
import { useState } from "react";
import { toast } from "sonner";

export default function Audit() { const workspaces = trpc.workspace.list.useQuery(); const [selected, setSelected] = useState<number | undefined>(); const workspaceId = selected ?? workspaces.data?.[0]?.id; const audit = trpc.audit.list.useQuery({ workspaceId: workspaceId! }, { enabled: Boolean(workspaceId) }); const evidence = trpc.audit.evidence.useQuery({ workspaceId: workspaceId! }, { enabled: Boolean(workspaceId) }); const utils = trpc.useUtils(); const upload = trpc.audit.uploadEvidence.useMutation({ onSuccess: () => { utils.audit.evidence.invalidate(); utils.audit.list.invalidate(); toast.success("Evidence artifact stored with a workspace-scoped reference."); }, onError: error => toast.error(error.message) }); const uploadFile = (file?: File) => { if (!file || !workspaceId) return; if (file.size > 5_000_000) { toast.error("Maximum evidence file size is 5 MB."); return; } const reader = new FileReader(); reader.onload = () => { const base64 = String(reader.result).split(",")[1]; if (base64) upload.mutate({ workspaceId, fileName: file.name, contentType: file.type || "application/octet-stream", contentBase64: base64 }); }; reader.readAsDataURL(file); }; return <div className="mx-auto max-w-7xl space-y-6"><header><Eyebrow>Chain of custody</Eyebrow><h1 className="mt-2 font-display text-4xl font-black uppercase tracking-[-.05em] text-white">Evidence <span className="neon-pink">Ledger</span></h1><p className="mt-3 max-w-2xl text-sm leading-6 text-slate-400">Every decision, rehearsal result, and future evidence reference is retained as a workspace-scoped audit record with a SHA-256 digest and timestamp. Large artifacts are kept in controlled storage by reference, not in the database.</p></header><div className="flex flex-col gap-4 sm:flex-row"><select value={workspaceId ?? ""} onChange={event => setSelected(Number(event.target.value))} className="h-10 max-w-md rounded-sm border border-cyan-300/20 bg-[#0a0d19] px-3 text-sm text-slate-200" disabled={!workspaces.data?.length}><option value="">Select workspace</option>{workspaces.data?.map(workspace => <option value={workspace.id} key={workspace.id}>{workspace.name}</option>)}</select><div className="flex items-center gap-2 text-xs text-slate-500"><ShieldCheck className="h-4 w-4 text-cyan-300" /> Retention controls are recorded per workspace.</div><label className="inline-flex h-10 items-center justify-center border border-fuchsia-400/40 px-4 text-sm text-fuchsia-200 transition hover:bg-fuchsia-400/10">{upload.isPending ? "Storing…" : "Store artifact"}<input className="sr-only" disabled={!workspaceId || upload.isPending} type="file" accept="image/*,.pdf,.txt,.json,.md" onChange={event => uploadFile(event.target.files?.[0])} /></label></div><div className="grid gap-6 xl:grid-cols-[1.2fr_.8fr]"><NeonFrame className="p-5 sm:p-6"><div className="flex items-center justify-between"><div><Eyebrow>Immutable-style event trail</Eyebrow><h2 className="mt-2 font-display text-2xl font-bold text-white">Audit events</h2></div><Hash className="h-5 w-5 text-cyan-300" /></div><div className="mt-5 space-y-3">{audit.data?.length ? audit.data.map(event => <div className="border-l-2 border-fuchsia-400/60 bg-white/[.015] p-4" key={event.id}><div className="flex flex-wrap justify-between gap-2"><p className="font-mono text-[10px] uppercase tracking-[.14em] text-cyan-300">{event.category} · {event.subject}</p><p className="text-xs text-slate-500">{new Date(event.createdAt).toLocaleString()}</p></div><p className="mt-3 break-all font-mono text-xs text-slate-400">sha256:{event.evidenceHash}</p></div>) : <p className="py-12 text-center text-slate-500">Select a workspace to inspect the audit trail.</p>}</div></NeonFrame><NeonFrame className="p-5 sm:p-6"><div className="flex items-center justify-between"><div><Eyebrow>Secure references</Eyebrow><h2 className="mt-2 font-display text-2xl font-bold text-white">Evidence exports</h2></div><FileLock2 className="h-5 w-5 text-fuchsia-300" /></div><div className="mt-5 space-y-3">{evidence.data?.length ? evidence.data.map(item => <div className="border border-cyan-300/10 p-3" key={item.id}><p className="font-mono text-[10px] uppercase text-cyan-300">{item.artifactType}</p><p className="mt-2 break-all text-xs text-slate-400">{item.storageReference}</p><p className="mt-3 break-all font-mono text-[10px] text-slate-500">sha256:{item.sha256}</p></div>) : <p className="py-12 text-center text-sm leading-6 text-slate-500">No artifact reference yet. Upload a screenshot, report, export, or text evidence up to 5 MB to store it safely under the selected workspace.</p>}</div></NeonFrame></div></div>; }
